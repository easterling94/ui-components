import { test, IDotToPlot } from './components/loaders/loader-test';

console.log(test + 10)

const testo: IDotToPlot = {
  name: 10,
  coordinates: {
    x: 10,
    y: 20
  }
}

console.log(testo.name + 30)
